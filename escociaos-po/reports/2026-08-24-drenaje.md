# Corrida 2026-08-24-drenaje (W0 + W1)

Ejecución del `runbooks/drenaje-backlog.md` en sesión interactiva, autorizada por Santiago.
Modo: **full write** · Notion operativo. Alcance autorizado: **W0 y W1**. No se tocó
ningún dato, ninguna migración se aplicó y no se fusionó nada.

## Resumen

Se ejecutaron las dos olas autónomas del runbook. **W0 desbloqueó 4 hallazgos** y separó
otros 4; **W1 abrió 5 PRs, todos verdes**. Y durante la corrida Santiago hizo el
despliegue por su cuenta, lo que cerró el P1 más viejo de la operación.

Backlog: 25 → **24 abiertos** (#36 cerrado). Con los 5 PRs fusionados bajaría a ~21;
el resto espera G2.

## W0 — Re-triage del flag `Requiere aprobación`

**Corrección de la premisa del runbook**: son **18** con el flag, no 19. (18 YES / 7 NO.)
No cambia la conclusión —sigue siendo el 72% del tablero— pero era una cifra concreta y
estaba mal en el runbook y en el cuerpo del PR #147. Corregida en la rama.

**4 desbloqueados** (flag quitado, ahora elegibles para el carril desatendido):

| # | Criterio |
|---|---|
| **#20** | La política ya está elegida en los otros 6 buckets. No hay nada que decidir |
| **#16** | Columna + trigger, patrón 040/050/063/074 ya aplicado 4 veces. Cero filas tocadas |
| **#19** | Tabla con **0 filas** a la que **nada escribe**. Alcance recortado (ver abajo) |
| **#37** | Filas afectadas esperadas **cero**, pero es el de mayor riesgo de ejecución |

**4 separados, flag conservado** (#11 #45 #12 #42): empaquetaban decisión + código. Se
escribió el corte en cada fila en vez de crear filas nuevas — el flag se queda porque la
mitad que decide es real, y la mitad de código se ejecutó bajo el go de esta sesión.

**Tres cosas que el re-triage destapó y no estaban en ningún hallazgo:**

1. **#37 tiene una trampa.** Esas 4 tablas **no tienen ninguna política de
   Gerencia/Administrador**: la always-true es el *único* camino de borrado, y es lo que
   hace funcionar el borrar-y-reinsertar de `CalculadoraAplicaciones.tsx:492/501/505`.
   Acotarla por propietario —el reflejo natural— **rompe producción**, y ninguna de las
   4 tiene `created_by`. Va por rol, obligatoriamente.
2. **#19 estaba sobredimensionado**: mezclaba «endurecer `logs_auditoria`» (trivial) con
   «no existe historial de cambios general» (decisión de producto). Recortado.
3. **#42 esconde una decisión del dueño**: subir el umbral de cobertura a 275 **descarta
   lluvia real** (2026-07-09, 28,19 mm sobre 268 lecturas). Recomendación: no tocar ese
   día — un contador truncado da cota inferior, jamás un total. Pero es criterio suyo.

## W1 — 5 PRs, todos verdes

| PR | # | Qué | Cierra al fusionar |
|---|---|---|---|
| **#148** | #35 | Documenta migraciones 094/095/096 en `CLAUDE.md`. Sustituye a #118 | **sí** (es docs) |
| **#149** | #45b | `calculosCierreAplicacion` importa `DIAS_LABORALES_MES` | no (queda #45a) |
| **#150** | #11a | Secreto del webhook de Telegram + 3 ids redactados + guarda | **no — necesita deploy propio** |
| **#151** | #12a | Umbral de gravedad al criterio compartido | no (quedan las 48 filas) |
| **#147** | — | El runbook mismo | — |

Línea base real de `main` (`6369352`): **129 ficheros / 2.951 tests**, lint 0 errores /
904 warnings. El runbook decía 128/~2.934; `main` avanzó con el merge de #146.

## Lo que Santiago hizo en paralelo: el despliegue

**#36 CERRADO, verificado vivo** (no contra el commit):

- `cron.job` jobid 1 ya manda `x-clima-sync-secret` → migración 105 aplicada
- `list_edge_functions` → **v215** (era 213), desplegada 2026-08-24
- Sonda con controles: `/clima/sync` anónimo → **401** (esta mañana **200 con escritura
  efectiva**); `/ruta/que/no/existe` → 404; `/hato/alertas/tick` → 401
- **El clima no perdió un tick**: últimos 6 ticks 200 con `synced:1`, última lectura hace
  1 minuto, 423 filas en ventana. Los dos valores del secreto coincidían — el bucle de
  401 silencioso, que era el riesgo real del orden, no ocurrió

**Pero no cierra #11**: el arreglo del webhook se escribió *después* de ese deploy. v215
no lo trae. Necesita otro, con su propia trampa: Telegram sólo manda el encabezado tras
`setWebhook(..., {secret_token})`, así que entre el deploy y ese paso el bot queda mudo
en 401 (reintenta, no se pierden mensajes, pero van seguidos).

## Dos errores propios, sin adornar

**1. Contaminé un PR.** Creé la rama del runbook en el checkout compartido y despaché los
agentes mientras estaba activa, así que #148 nació con mis 253 líneas del runbook dentro.
El agente lo detectó y lo limpió antes de que yo llegara. **Es exactamente el riesgo que
había señalado media hora antes y luego provoqué.** La regla ya está escrita en la memoria
compartida desde el 2026-08-03; lo que faltaba era aplicármela a mí, no sólo a los agentes.

**2. Mi barrido de secretos tenía un punto ciego.** El agente de #150 encontró un **tercer
chat id** que mi hallazgo no tenía (`docs/plan_hato_telegram_estados_agosto_2026.md:52`,
de otro Administrador) y **uno más en `escociaos-po/memory/usage-analytics.md:69`** — un
fichero de la propia operación. Verificado: entró el 2026-08-10 (`8aae063`), no lo
introduje yo, y mi commit de hoy no añadió ningún literal. Pero el P0 de mi corrida era
literalmente «hay chat ids en el repo público», mi barrido miró `src/` y `docs/` y **nunca
miró `escociaos-po/`**, y el id sobrevivió dos corridas. La guarda de #150 ya incluye ese
directorio: queda cerrado por mecanismo, no por acordarse.

## NO CORRIÓ / no se hizo

- **G1 y G2**: G1 lo resolvió Santiago por fuera. G2 sigue pendiente — 7 decisiones y el
  paquete de datos, agenda escrita.
- **W3 (`ddl_aditivo`)**: no se ejecutó. Los 4 quedaron *elegibles*, pero aplicar una
  migración necesita una propuesta filada y un go por ítem, y el alcance autorizado hoy
  era W0+W1.
- **W4**: bloqueada por G2, como está diseñado.
- **#23, #28, #40, #41, #42a**: elegibles y no trabajados — se priorizaron los 5 de mayor
  valor para mantener la revisión manejable. Quedan para la próxima.

## Criterio de parada aplicado

El runbook dice parar cuando no queda nada elegible sin compuerta. Se paró ahí: todo lo
que sigue necesita, o tu decisión (G2), o una propuesta escrita previa (W3, datos).
