Escocia OS — corrida 2026-09-03-jueves · modo: full write

RESUMEN (ES)
Semana silenciosa en el producto —cinco commits, ninguno de código de la aplicación— y las
dos alarmas altas que quedaron abiertas el lunes se cerraron solas antes de esta corrida: el
servidor de la ronda de inventario volvió a producción el 1 de septiembre, seis horas antes
del arranque del mes, y el cierre del escribir libre en 20 tablas ya está aplicado. Lo que sí
apareció es la razón por la que la ronda de septiembre lleva dos días sin abrirse: un
suscriptor de Telegram sin vincular entra en la lista de destinatarios como el texto "null",
el sistema da el recordatorio por enviado y no lo repite. Hay PR verde para el código; la
parte de datos necesita tu visto bueno, y el código de vinculación de Uriel vence mañana.

P0: 0 · P1: 0 · P2: 3 · P3: 1   (nuevos)  |  cerrados: 2

REQUIERE TU DECISIÓN
- Recordatorio de la ronda de septiembre ya consumido → o abres la ronda a mano / Uriel canjea
  su código antes del 2026-09-04, o autorizas el borrado de UNA fila de `rondas_avisos`. La
  propuesta está filada con SQL exacto, rollback, chequeo de pre-estado y conteo esperado (1).
  Recomiendo la vía sin escritura: abrir la ronda a mano. Desplegar el PR #191 ANTES, o el
  siguiente tick vuelve a consumir la clave igual.
- Jornales que suman más de 1,0 en un día (42 casos, $2,3M) → la pregunta que decide es tuya:
  ¿es horas extra legítimas o siempre error de captura? El ENUM sólo tiene 4 etiquetas y la
  migración 106 ya estableció que registrar horas extra exige una etiqueta nueva, no un cambio
  de UI. No se toca ninguna de las 42 filas hasta que respondas.
- `escociaos-po/CHANGELOG.md` nunca se creó porque el brief y la constitución se contradicen.
  Tres salidas ya escritas; recomiendo la (b), moverlo a `escociaos-po/reports/CHANGELOG.md`.

PRs ABIERTOS
- fix(telegram): skip unlinked subscribers when resolving alert recipients →
  https://github.com/sforero94/Escociaos/pull/191
  Verde: tsc limpio, lint 0 errores, 155 ficheros / 3.393 tests. Toca los DOS árboles de edge
  function, así que **no surte efecto hasta desplegar**, y el despliegue se verifica por
  contenido del bundle, nunca por `updated_at`.
- (ajeno a la operación) #190 «Informes de visita agronómica», tuyo, abierto esta mañana.
  Trae la migración 134 sin aplicar y toca los dos árboles de edge function.

NO CORRIÓ
- Nada. Los 4 agentes del roster del jueves completaron.
- `apply_migration` no se probó: dormante a propósito en una corrida desatendida.

---

## Cierres

**[P1] 20 tablas con políticas INSERT/UPDATE always-true** → Arreglado.
Migración 133 fusionada (PR #188, `297a230`, 2026-09-01T02:18:11Z) y aplicada **5 min 24 s
después** (ledger `20260901022335`). El reloj de 7 días nunca arrancó. Barrido canónico del
propio hallazgo sobre `pg_policies`: **0** (eran 33). `has_table_privilege('anon', …,
'INSERT'/'UPDATE')`: **0 de 20 tablas** (eran 20 de 20). Cero filas afectadas.

**[P1] La v236 republicó el bundle pre-inventario v224** → Arreglado, y **el daño predicho no
ocurrió**. La v238 restauró la superficie servidor el 2026-09-01T01:53:38Z, diez horas antes
del tick de arranque de septiembre. Cerrado por dos agentes independientes con métodos
distintos: inventario de ficheros (71 contra 71 alcanzables; la v236 daba 58), diff de los 71
ficheros compartidos (0 difieren), la fila de dominio `recordatorio:2026-09` escrita a las
12:00:03Z, y una sonda anónima idempotente con control positivo y negativo.
**Costo real del apagón: exactamente un tick, el del 2026-08-31** — visible como el único hueco
en `rondas_avisos` entre el 08-30 y el 09-01, mientras `cron.job_run_details` decía `succeeded`.

## Actualizaciones (evidencia nueva, sin fila nueva)
- **2026-08-28 ausente de `clima_resumen_diario`**: ya no son 3 corridas fallidas, son **6**. La
  ventana de 21 días del reintento vence el **2026-09-18**; después el día es irrecuperable.
- **`lecturas_count` > 288**: vigente y **no propagado**; ahora son dos días (08-27 = 349,
  08-29 = 311). Todos los días posteriores marcan exactamente 288.
- **Migraciones mal documentadas**: ya no son 4, son **5**, y la más nueva quedó mal documentada
  a los cinco minutos de escribirse. La documentación no se atrasa por semanas, se atrasa por
  minutos, porque aplicar tiene mecanismo y documentar no.
- **Ronda semana 1 / segregación de funciones**: subió la urgencia, no la severidad. El código de
  Uriel vence el 2026-09-04 y su falta de vinculación resultó ser la causa del arranque perdido.

## Refutado antes de filar (la compuerta funcionando barata)
- 10 grupos «duplicados» de `registros_trabajo` → todos con `lote_id` distinto, que es el modelo
  correcto. La clave de duplicado incluye `lote_id` y `tarea_id`, siempre.
- 83 acciones recomendadas rancias → el consumidor filtra por `corrida_id`; nunca se renderizan.
- 64 filas nuevas de `globalgap_correcciones` → recaptura, no pérdida: los conteos recapturados
  coinciden exacto (20 mdp / 34 mdt).
- La alerta `parto_proximo` de hoy → correcta, no disparó sobre dato rancio.

## Changelog de la semana (texto; no se commitea como fichero — ver el P3)

### Para el usuario
Nada. Ninguna pantalla cambió y ninguna función nueva llegó al campo: cero ficheros de `src/`
en los 5 commits de la ventana.

### Seguridad · Base de datos
Escribir en 20 tablas deja de estar abierto a cualquier cuenta autenticada. Insertar y
actualizar monitoreos, producción, lotes, sublotes, registros de trabajo, movimientos diarios
y la cadena GlobalGAP ahora exige rol Gerencia o Administrador; la llave `anon` del navegador
pierde el permiso directo en las 20. Cero filas afectadas; una sola cuenta de diez pierde
capacidad, y es la cuenta Verificador que todavía no ha entrado nunca.
(migración 133, PR #188 · fusionada 02:18Z · aplicada 02:23Z)

### Despliegue
El módulo de ronda de inventario volvió a tener servidor. El despliegue del 30-ago había
republicado un bundle del 28-ago, dejando 17 ficheros fuera de producción. La v238 los devolvió
el 01-sep a las 01:53Z. Costo comprobado: un solo tick perdido, el del 31-ago.

### Interno
Preferencia de estilo de respuesta (STE100) y su equivalente en español, en `CLAUDE.md`.
Memoria y reporte de la corrida 2026-08-31-lunes.

### Requiere despliegue manual
El PR #191, una vez fusionado. Nada más pendiente.

## Estado de despliegue y deriva
| | |
|---|---|
| HEAD | `297a230` = `origin/main`, árbol limpio |
| Migraciones | 001–133 todas aplicadas. **Cero fusionadas-sin-aplicar, cero aplicadas-sin-fusionar** |
| Edge function | **v238**, hash `487359f9…`, byte-idéntica al árbol de `297a230` |
| Frontend | Vercel 4/4 READY, alias PROMOTED en `297a230` |
| Crons | 6/6 disparando; `ronda-inventario-tick` con efecto comprobado en `rondas_avisos` |
| Backlog | 17 → **19** abiertos (4 nuevos, 2 cerrados). 0 estancados (>60 días) |
