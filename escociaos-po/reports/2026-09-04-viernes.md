Escocia OS — corrida 2026-09-04-viernes · modo: full write

RESUMEN (ES)
Drenaje limpio: tres defectos del backlog quedaron arreglados y con PR verde, todos
reproducidos primero con una prueba en rojo. Los tres son de campo — dos de la ronda de
inventario (un conteo fisico imposible que se podia confirmar, y el borrador de voz que se
tragaba los mensajes que le escribias a Esco) y uno del clima (el reintento diario que
falla en silencio sobre el 28 de agosto). Nada se aplico a la base de datos: el unico
hallazgo que lo pedia lleva tu visto bueno pendiente. Lo que necesita decision tuya son
cuatro cosas chicas, y una tiene fecha: el 28 de agosto deja de ser recuperable el 18 de
septiembre.

P0: 0 · P1: 0 · P2: 0 · P3: 0   (nuevos)  |  cerrados: 0 · drenados a PR: 3

DRENADO
- 3 hallazgos → PR abierto · 0 migraciones aplicadas · 0 cerrados por obsoletos
- quedan 5 elegibles sin tocar (cap del dia: 3 PRs de codigo)

APLICADO SIN FUSIONAR
- Ninguna. Cero migraciones aplicadas esta corrida, y el estado heredado tambien esta
  limpio: ficheros 120-136 en `main`, ledger hasta 136.

REQUIERE TU DECISIÓN
- **La ventana de 30 minutos del borrador de la ronda (PR #196) es eleccion nuestra, no
  tuya.** Un borrador de voz sin confirmar deja de interceptar texto pasados 30 min. El
  caso que rompio tardo 2 h 45 min; el que si se confirmo, 30 segundos. Cambiarlo es una
  constante y un numero en el test. Confirmame el numero.
- **El 28 de agosto de clima vence el 2026-09-18.** El PR #195 arregla el silencio, no
  recupera el dia. Recuperarlo exige disparar `POST /clima/backfill` para **ese solo dia,
  una sola llamada, nunca en paralelo**, que escribe datos y por eso no lo hace una corrida
  desatendida. Si Ecowitt contesta «sin datos», el estado final correcto es **una fila
  marcada explicitamente como sin dato, no una fila ausente** — pero eso ya es decision
  tuya, no algo que se improvise.
- **Reclasificar el residuo de ESCO-60 a `ddl_aditivo`.** El PR #194 cierra el unico
  productor conocido de un conteo negativo, pero **la base sigue aceptando uno de
  cualquier otro escritor**: `rondas_excepciones` no tiene `CHECK` sobre `cantidad_fisica`
  (0 de 9 constraints) y el cuerpo vivo de `fn_ronda_confirmar_hallazgos` solo comprueba
  `IS NULL`. La 132 puso esa misma guarda un estado mas adelante, asi que el paso de
  confirmacion es la asimetria. Filas afectadas: **cero** (la unica fila real es 3).
  Reclasificado queda elegible para el carril desatendido del proximo viernes.
- **El punto 4 de la accion de ESCO-62 quedo REFUTADO y deberia filarse como `decision`,
  no como bug.** Proponia descartar una correccion cuyo `producto_confianza` fuera
  `ninguna`. El agente comprobo que **esa senal se parsea y no la consume nadie**, que
  convertirla en guarda choca con el §7.3 del contrato del modulo (impediria alcanzar
  `MAX_INTENTOS_PREVIEW` y mataria el terminal `sin_confirmar`), y que **no habria
  atrapado este defecto**. El viernes no fila hallazgos nuevos, asi que te lo dejo aca.

PRs ABIERTOS
- fix(inventario): never derive a negative physical count in the ronda preview →
  https://github.com/sforero94/Escociaos/pull/194
- fix: log the days the clima retry cron fails to recover →
  https://github.com/sforero94/Escociaos/pull/195
- fix(inventario): bound the ronda draft's text-correction window to 30 minutes →
  https://github.com/sforero94/Escociaos/pull/196
- (heredado del jueves, sin fusionar) fix(telegram): skip unlinked subscribers →
  https://github.com/sforero94/Escociaos/pull/191

**LOS CUATRO EXIGEN REDESPLIEGUE DE EDGE FUNCTION, y fusionar no los pone en produccion.**
`npx supabase functions deploy make-server-1ccce916`, precedido de `git fetch && git checkout
<sha de main>` y **verificado por CONTENIDO del bundle, jamas por `updated_at`** — un
despliegue desde un checkout viejo sube la version igual, y ya paso dos veces en tres dias
(v223 y v236).

NO CORRIÓ
- **Carril `ddl_aditivo`** — no por fallo: **no habia una sola migracion elegible**. Ver abajo.
- `apply_migration` no se probo (nada que aplicar). `COMPOSIO_MULTI_EXECUTE_TOOL` tampoco:
  el drenaje no lee Vercel.
- Ningun agente quedo fuera. Los 3 despachados completaron.

---

## Fase 0 — Arranque

| | |
|---|---|
| Run id | `2026-09-04-viernes` |
| Modo | full write · Notion OPERATIVO |
| HEAD al arrancar | `01e3690` = `origin/main` |
| Dead-man | **OK** — corrida anterior 2026-09-03-jueves, hueco de **1 dia** |
| Conector diagnostico | `Supabase` (solo lectura) — verificado en vivo: `supabase_read_only_user`, `default_transaction_read_only = on` |
| Conector escritura | `Supabase_Escritura` (`apply_migration` y nada mas) — **no se uso** |

### La regla del arbol local se gano el sitio por CUARTA vez

El checkout arranco con `HEAD` correcto pero la **rama local `main` clavada en `f98f83a`,
106 commits atras** — identica a la trampa del 2026-08-20, del 08-27 y del 09-03. Corregida
**antes de despachar** con `git branch -f main origin/main`, que mueve el ref sin tocar el
arbol de trabajo.

Cuatro corridas seguidas con la misma condicion ya no es mala suerte: **el sandbox se clona
con la rama local desactualizada por construccion.** Deja de ser una trampa que detectar y
pasa a ser un paso de arranque obligatorio.

### Preflight de tools

| Tool | Resultado |
|---|---|
| `execute_sql` (solo lectura) | OK |
| `query_logs` | OK — fue la evidencia decisiva de ESCO-64 |
| Notion (`query-data-sources`, `fetch`, `update-page`) | OK |
| github (`list_pull_requests`, `pull_request_read`, `create_pull_request`) | OK |
| `apply_migration` | **NO PROBADO** — sin migracion elegible |

**Cero prompts de permiso en toda la corrida.**

## Fase 0b — El conjunto elegible

Backlog abierto al arrancar: **19 hallazgos**. Filtro mecanico del runbook (`Severidad`
P2/P3 · `Estado` Not started · `Confianza` Alta · `Requiere aprobacion` sin marcar · `Clase`
codigo o ddl_aditivo):

**8 elegibles — los 8 `codigo`, los 8 P2, los 8 esfuerzo S.**

Los 8 quedaron empatados en el criterio `Severidad × Esfuerzo⁻¹` del runbook, asi que el
desempate fue por impacto y por **evitar solape de ficheros entre agentes en vuelo**:

| Drenado | Hallazgo | PR |
|---|---|---|
| ✅ | ESCO-62 · el borrador de voz se traga el texto posterior | #196 |
| ✅ | ESCO-60 · conteo fisico negativo confirmable | #194 |
| ✅ | ESCO-64 · el reintento de clima falla en silencio | #195 |

**5 elegibles quedaron sin tocar por el cap de 3 PRs**, no por descarte: unidades de medida
ausentes en el informe de cierre · `fn_ronda_actor_nombre` escribiendo nombre en vez de
correo · la cuarta copia del umbral de gravedad en el bot · el detector de deriva de
despliegue que solo compara marcas de tiempo · las migraciones mal documentadas en el
`CLAUDE.md` raiz. Son los primeros candidatos del proximo viernes.

### Cero elegibles `ddl_aditivo`, y esa es la parte que conviene mirar

El backlog tiene **exactamente un** hallazgo `ddl_aditivo` —los tres crones que expiran en
`pg_net` a los 5 s— y lleva **`Requiere aprobacion` marcado**. §5 dice que ese flag anula
`clase` por completo, asi que el unico candidato de la clase es inelegible por construccion
y **el carril desatendido mas valioso de la operacion se quedo parado sin tener nada roto.**

Es el mismo cuello de botella que documento el drenaje del 2026-08-24 («el flag era el cuello
real, no el volumen»). Vale la pena revisarlo en el proximo re-triage.

## Fase 1 — Lo que se arreglo

Los tres agentes siguieron el mismo orden obligatorio: reproducir contra `origin/main`,
escribir la prueba **antes** del arreglo y verla en rojo, arreglar, y confirmar verde.

**ESCO-60 → PR #194.** `derivarFisico` restaba sin piso, asi que una narracion «faltan N»
con N mayor que el teorico congelado producia un fisico negativo con `[Confirmar]` activo.
Rojo sobre el valor literal en vivo (`-150` del 15-15-15). El arreglo devuelve `'incompleto'`
y **no recorta a 0 a proposito**: un 0 recortado es verosimil y esconde un mal parseo dentro
de un registro de trazabilidad firmado. No hizo falta tocar `preview.ts` ni Telegram — el
camino `'incompleto'` ya existia.

**ESCO-62 → PR #196.** `obtenerTranscritoPendienteMasReciente` no tenia predicado de tiempo,
y el handler de texto **consume** el mensaje sin llamar a `next()`. Acotado a 30 min, con la
constante exportada y greppable, y con aviso explicito al usuario nombrando la nota por su
hora de Bogota. **CA-37 se preserva**: la fila no se muta, sigue contando como hallazgo
narrado sin confirmar, y los botones siguen resolviendo por `transcrito_id` sin ventana.

**ESCO-64 → PR #195.** La evidencia decisiva no estaba donde decia el hallazgo: en los logs
de la edge function, la corrida anuncia `4 dia(s) candidato(s)` y solo tres dejan linea de
resultado. El cuarto —el 28 de agosto— se evapora. Y el bucle **manual** de backfill si
registra sus fallos, asi que la asimetria prueba que el silencio del automatico es defecto y
no diseno. Una linea de `console.error` en los dos arboles.

### Comprobacion de contaminacion — y una correccion a como se hace

Las tres ramas: limpias. Cada fichero mapea a su hallazgo, los dos arboles de edge function
quedaron sincronizados, y **ningun agente toco los ficheros reservados por otro**.

Pero la regla escrita el 2026-08-24 (`git diff --name-only origin/main origin/<rama>`) **es un
diff de dos puntos y hoy dio un falso positivo**: el commit del reporte parcial movio
`origin/main`, y las tres ramas —cortadas antes— aparecieron *borrando* el fichero del
reporte, que ninguna habia tocado. **La comprobacion correcta es de tres puntos
(`origin/main...<rama>`)**, que diffea contra la base de fusion y muestra lo que la fusion
realmente introduce. Corregido en la memoria.

### El CI de este repo no prueba codigo

Los tres PRs traen **un solo check: el despliegue de preview de Vercel**. No hay workflow de
tests en Actions. **El verde de GitHub no dice que la suite pase.** La prueba real es la que
corrio cada agente en su worktree y transcribio al cuerpo del PR: rojo primero, verde despues.

| PR | Suite | Lint | Typecheck |
|---|---|---|---|
| base `main@01e3690` | 157 ficheros / 3.433 tests | 0 errores / 906 warnings | limpio |
| #194 | 157 / 3.435 | 0 / 906 | limpio |
| #195 | 158 / 3.438 | 0 / 906 | limpio |
| #196 | 158 / 3.445 | 0 / 906 | limpio |

Ninguno de los tres anade un solo warning de lint.

## Fase 2 — Carril de migracion

**No corrio.** Conjunto elegible `ddl_aditivo` vacio (ver Fase 0b). Ninguna de las cinco
compuertas llego a evaluarse, y **no se aplico nada a produccion**.

## Fase 4 — Pase de antiguedad

**Vacio.** El hallazgo abierto mas viejo se detecto el **2026-08-03** (32 dias). Ninguno
alcanza el umbral de 60 dias, asi que la corrida **no propone ninguna aceptacion**.

## Higiene de la base de hallazgos

Una fila quedaba inconsistente del jueves: el hallazgo del destinatario `"null"` de Telegram
tenia el PR #191 enlazado pero seguia en `Not started`. Corregido a `In progress`. Los tres
drenados hoy quedaron en `In progress` con su PR enlazado.

## Estado de despliegue y deriva

| | |
|---|---|
| HEAD | `4e623cf` = `origin/main` |
| Migraciones | 120-136 en `main`, ledger hasta `20260903182508`. **Cero fusionadas-sin-aplicar, cero aplicadas-sin-fusionar** |
| Backlog | 19 abiertos, **sin cambio** (el viernes no fila). 3 pasaron a `In progress` |
| Estancados (>60 dias) | 0 |

## Racha del viernes (regla de auto-poda)

| Corrida | Conjunto elegible | Racha de vacios |
|---|---|---|
| 2026-08-28-viernes | 6 elegibles | 0 |
| **2026-09-04-viernes** | **8 elegibles** | **0** |

La auto-poda no aplica: dos viernes, los dos con trabajo real que drenar.

## Desviaciones registradas

- **Los briefs y las memorias se inyectaron por lectura obligatoria de ruta, no pegados en el
  prompt.** La constitucion §3 manda pasar el cuerpo completo; los ficheros existen en este
  checkout, asi que cada agente los leyo como primer paso y se verifico en sus reportes. Se
  anota por transparencia — el contrato se cumple, el mecanismo cambia.
- **Los worktrees de agente no se retiraron.** Estan en `.gitignore`, no contaminan el commit
  de memoria, y esta corrida no corrio suite de verificacion en el checkout compartido (cada
  agente verifico en el suyo). La regla de retirarlos sigue vigente para cuando si se corra.
