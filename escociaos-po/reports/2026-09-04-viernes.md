Escocia OS — corrida 2026-09-04-viernes · modo: full write

RESUMEN (ES)
(pendiente — se completa al cierre de la corrida)

P0: 0 · P1: 0 · P2: 0 · P3: 0   (nuevos)  |  cerrados: —

---

> **NOTA DE ESTADO**: este fichero se escribio al arrancar la Fase 1 y se completa al
> cierre. Si quedo asi, la sesion murio con agentes fuera — ver NO CORRIO.

## Fase 0 — Arranque

| | |
|---|---|
| Run id | `2026-09-04-viernes` |
| Modo | full write · Notion OPERATIVO |
| HEAD | `01e3690` = `origin/main`, arbol limpio |
| Dead-man | **OK** — corrida anterior 2026-09-03-jueves, hueco de **1 dia** |
| Conector diagnostico | `Supabase` (solo lectura) — verificado en vivo: `supabase_read_only_user`, `default_transaction_read_only = on` |
| Conector escritura | `Supabase_Escritura` (`apply_migration` y nada mas) — **NO se uso esta corrida**, ver Fase 2 |

### La regla del arbol local se gano el sitio por CUARTA vez

El checkout arranco con `HEAD = 01e3690` (correcto, = `origin/main` tras el fetch) pero la
**rama local `main` clavada en `f98f83a`, 106 commits atras** — identica a la trampa del
2026-08-20, del 08-27 y del 09-03. Corregida **antes de despachar** con
`git branch -f main origin/main`, que mueve el ref sin tocar el arbol de trabajo.

Cuatro corridas seguidas encontrando la misma condicion no es mala suerte: es que el
sandbox se clona con la rama local desactualizada por construccion. **Ya no es una trampa
que evitar, es un paso de arranque obligatorio.**

### Preflight de tools

| Tool | Resultado |
|---|---|
| `execute_sql` (solo lectura) | OK |
| Notion (`notion-query-data-sources`, `notion-fetch`, `notion-update-page`) | OK |
| github (`list_pull_requests`) | OK |
| `apply_migration` (`Supabase_Escritura`) | **NO PROBADO** — no hubo migracion elegible que aplicar (ver Fase 2) |

Cero prompts de permiso.

## Fase 0b — El conjunto elegible

Backlog abierto al arrancar: **19 hallazgos**. Aplicando el filtro mecanico del runbook
(`Severidad` P2/P3 · `Estado` Not started · `Confianza` Alta · `Requiere aprobacion`
sin marcar · `Clase` codigo o ddl_aditivo):

**8 elegibles, los 8 `codigo`, los 8 P2, los 8 esfuerzo S.**

**Cero elegibles `ddl_aditivo`.** El unico hallazgo de esa clase en todo el backlog —
los tres crones que expiran en pg_net a los 5 s — lleva `Requiere aprobacion` marcado, y
§5 dice que ese flag anula `clase` por completo. Asi que **el carril de migracion aditiva
no corre esta semana**, y no por falta de trabajo sino porque el unico candidato necesita
una decision de Santiago.

(resultados por hallazgo: pendiente)

## Fase 4 — Pase de antiguedad

**Vacio.** El hallazgo abierto mas viejo se detecto el **2026-08-03** (32 dias). Ninguno
alcanza el umbral de 60 dias, asi que la corrida **no propone ninguna aceptacion**.

## Estado de migraciones

**LIMPIO.** Ficheros 120-136 presentes en `main`; el ledger llega a `20260903182508`
(136). **Cero fusionadas-sin-aplicar y cero aplicadas-sin-fusionar.**

## Racha del viernes (regla de auto-poda)

| Corrida | Conjunto elegible | Racha de vacios |
|---|---|---|
| 2026-08-28-viernes | 6 elegibles | 0 |
| 2026-09-04-viernes | **8 elegibles** | **0** |

La auto-poda no aplica: dos viernes, los dos con trabajo real que drenar.
