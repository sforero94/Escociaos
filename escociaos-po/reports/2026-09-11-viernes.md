# Escocia OS — corrida `2026-09-11-viernes` (drenaje del backlog)

**Modo: full write · Notion OPERATIVO · preflight 5/5 sin un solo prompt de permiso.**

---

## RESUMEN (ES)

Se drenaron 3 de los 9 hallazgos elegibles con PR verde: el texto de las alertas de tratamiento del
hato, las unidades de medida que faltaban en el informe de cierre de la ronda, y la cuarta copia
escondida del umbral de gravedad. La cuarta corrección era una migración y **no se aplicó**: su propia
revisión adversarial la frenó al descubrir que el número ya estaba tomado por una rama tuya sin
fusionar, y que el cambio le habría puesto tu correo en vez de tu nombre a la firma del informe
mensual. Nada se escribió en producción.

De paso apareció algo que no venía en el backlog: **la suite de pruebas lleva dos días en rojo** por dos
migraciones que quedaron con el mismo número tras la renumeración del 9 de septiembre, y **la revisión
del jueves pasó por encima sin verlo**, porque a sus agentes se les había prohibido correr las pruebas.
Eso sí se filó — es un hallazgo contra la propia operación, la única clase que el viernes puede filar.

Esta semana necesitás decidir tres cosas: si se fusionan los tres PR de código, si se arregla el número
de migración repetido, y si el viernes sube su tope de trabajo — el backlog elegible va 6 → 8 → 9 tres
viernes seguidos contra un tope fijo de 4.

---

## P0: 0 · P1: 0 · P2: 1 · P3: 0  (nuevos)  |  cerrados: 0 · en progreso: 4

El único hallazgo nuevo es contra la operación. **El viernes no fila hallazgos de producto y no abrió
ninguna investigación.**

---

## DRENADO

- **3 hallazgos → PR abierto y verde** · **0 migraciones aplicadas** (la única candidata la frenó su
  propia revisión adversarial — ver abajo) · **0 cerrados por obsoletos**
- **quedan 5 elegibles sin tocar** por el tope del día (3 PR de `codigo` + 1 migración `ddl_aditivo`)

## APLICADO SIN FUSIONAR

**Ninguna.** No hay migración aplicada esperando fusión, así que no se abrió la ventana en la que
producción corre un esquema que `main` no documenta.

| # | Hallazgo | PR | Estado |
|---|---|---|---|
| 89 | La alerta de `tratamiento_paso` decía «para hoy» sobre un paso vencido en julio | [#219](https://github.com/sforero94/Escociaos/pull/219) | In progress |
| 61 | El informe de cierre de la ronda se contradecía: «hay 3» contra «Entrada de 150» | [#220](https://github.com/sforero94/Escociaos/pull/220) | In progress |
| 48 | Una cuarta copia del umbral de gravedad, invisible a toda guarda por llamarse distinto | [#221](https://github.com/sforero94/Escociaos/pull/221) | In progress |
| 63 | `fn_ronda_actor_nombre` guardaba un nombre donde la columna lleva correo | [#222](https://github.com/sforero94/Escociaos/pull/222) | In progress |

**Diferidos por el tope, ninguno descartado**: #66, #69, #71, #77, #78.

---

## La migración 142 se escribió, se revisó y NO se aplicó

Es el resultado más útil de la corrida, así que va completo.

El carril `ddl_aditivo` tiene cinco gates. **Dos fallaron**, la revisión adversarial independiente
devolvió **`unsafe`**, y **no se aplicó nada**: `fn_ronda_actor_nombre` sigue con el cuerpo de la 126.
Los dos fallos se re-verificaron después contra el repo y el catálogo vivo, para no fiarse de un solo
agente. El PR [#222](https://github.com/sforero94/Escociaos/pull/222) queda abierto y **comentado como
NO FUSIONAR tal cual**.

### Gate 4 — el número `142` ya estaba tomado

```
origin/claude/po-viernes-actor-nombre-correo -> 142_fn_ronda_actor_nombre_prefiere_correo.sql
origin/cursor/hato-gestor-alertas-fb81       -> 142_alertas_defaults_campo_secado_tratamiento.sql
```

La segunda es de tu PR #218 (issue #217), abierta y sin fusionar. Si las dos se fusionan, el repo queda
con **dos ficheros `142_*`** — el estado exacto que hoy tiene el `140` y que mantiene la suite en rojo.
Habríamos reproducido el bug del punto 1 de esta misma corrida, el mismo día.

**Lo que hace esto interesante**: el agente comprobó las **tres** fuentes que manda el runbook —nombres
de fichero en `main`, `supabase_migrations.schema_migrations`, y el esquema `respaldos`— y **las tres
estaban limpias**. La colisión vive en una **rama abierta**, que es el único sitio donde el runbook no
dice mirar. Es un defecto del runbook, no del agente, y ya se corrigió en la memoria.

### Gate 3 — la función tiene un tercer consumidor que `pg_proc` no puede ver

`fn_ronda_actor_nombre` no alimenta sólo `movimientos_inventario.responsable`. Se la llama **por RPC
desde TypeScript** para mostrarle un nombre a una persona: `ronda-helpers.ts:546` →
`resolverNombreActor()` → `telegram/bot.ts:1029` («Causa propuesta por …») y
`ronda-inventario-tick.ts:589` (`cerradoPorNombre`, **la línea de firma del informe de cierre**).

Con la migración aplicada, la firma de tu informe mensual pasaba de `-- Santiago Forero` a
`-- sforero94@gmail.com`, y el mensaje de Telegram de `Martha Vega` a `martik07@gmail.com`. Ése es
justamente el consumidor donde el nombre **es lo correcto**. Y el detalle que lo vuelve concreto: ese
`-- Santiago Forero` es literalmente el texto que aparece en la evidencia del hallazgo #61.

### El defecto real es más de fondo que el orden del COALESCE

Una sola función sirve a dos necesidades incompatibles: la columna de atribución quiere el **correo**,
el mensaje a un humano quiere el **nombre**. La forma correcta son **dos helpers** — dejar
`fn_ronda_actor_nombre` como está para presentación, y crear `fn_ronda_actor_correo` que usen sólo los
dos RPC que escriben `responsable`. Con número `143`, comprobado también contra ramas abiertas.

### Lo que la revisión intentó y NO pudo romper

Ocho ataques fallidos, y valen tanto como los dos que acertaron, porque son lo que haría creíble un
veredicto `safe`: firma, `prosecdef`, `provolatile`, `search_path`, ACL, owner y `oid` se preservan
exactos; la función **nunca puede devolver NULL**; ninguna política RLS la llama; `postgres` tiene
`rolbypassrls`, así que las guardas no eran vacías; el re-run aborta limpio con un mensaje accionable;
y no hay **ningún literal absoluto de conteo** contra `movimientos_inventario` (la trampa que costó la
103), tabla que hoy está en 165 filas y creciendo.

**El gate hizo exactamente su trabajo.** Van seis revisiones adversariales en la historia de esta
operación que cambian el resultado.

---

## REQUIERE TU DECISIÓN

### 1. `main` lleva dos días en rojo — y la revisión del jueves no podía verlo

Es el hallazgo nuevo de esta corrida (contra la operación, no contra el producto).

`src/sql/migrations/` tiene **dos ficheros con el prefijo `140`**
(`140_hato_registrar_tratamiento.sql` y `140_respaldo_pl_chequeo_vacas.sql`), residuo de la
renumeración que el propio `CLAUDE.md` narra en la entrada de la 140. La guarda
`hatoSchemaContract.test.ts` los detecta y falla desde `11c2a93` (2026-09-09).

Lo que hay que decidir no es eso, que es de una línea. Es lo otro: **la corrida del jueves corrió al
día siguiente y no lo vio, porque a sus agentes se les había prohibido correr `npm test`** — los cuatro
comparten un checkout e instalar dependencias les mueve el árbol a todos. Con la suite apagada, un
`main` rojo es invisible.

Hoy se resolvió dándole a cada agente **su propio `git worktree`** con `node_modules` enlazado por
symlink, y un solo `npm ci` antes de despachar. Coste: ~2 minutos. El rojo apareció en los primeros
cinco minutos y los cuatro agentes corrieron lint, typecheck y la suite entera sin pisarse.

**La decisión**: ¿fijo ese patrón en los tres runbooks? Cierra también el #85, que sigue abierto y
describe la mitad de escritura del mismo problema.

### 2. El viernes ya no drena más rápido de lo que lunes y jueves llenan

| Corrida | Elegibles | Tomados | Quedaron |
|---|---|---|---|
| 2026-08-28-viernes | 6 | 4 | 2 |
| 2026-09-04-viernes | 8 | 3 | 5 |
| **2026-09-11-viernes** | **9** | **4** | **5** |

La regla de auto-poda **no se dispara** (tres viernes seguidos con trabajo, que es lo contrario del
problema que esa regla vigila). Pero la serie sube y el tope es fijo. Las salidas: subir el tope de
`codigo` de 3 a 4 o 5, o aceptar explícitamente que el `codigo` P2/S se acumula. Los cinco diferidos de
hoy son todos P2, esfuerzo S, confianza Alta — o sea trabajo listo para hacer, no dudas.

### 3. Cuatro PR esperando tu fusión

El viernes nunca fusiona. Los cuatro están verdes y sin conflicto. **Tres de ellos requieren
`npx supabase functions deploy make-server-1ccce916` después de fusionar** (#219, #220, #221): sin el
despliegue, el bot de Telegram sigue con el código viejo. Es la lección del 2026-08-24 — «fusionado»
dejó de ser prueba de nada.

---

## Lo que NO se tocó, a propósito

- **La reparación de datos del #61** (la fila que dice 3 donde fueron 150) es `clase datos`. El agente
  no la ejecutó **ni la compuso**. Nadie estaba mirando esta corrida, y un «go» necesita una pregunta
  previa y una persona viva que la conteste.
- **La reparación de datos del #63** (la fila `'Santiago Forero'`) sigue igual, y sigue necesitando que
  digás **cuál de tus dos cuentas** actuó. Dato nuevo, por si ayuda: ese nombre coincide exactamente con
  el `nombre_completo` de `sforero94@gmail.com` **y** con el `nombre_display` de Telegram del mismo
  `usuario_id`; `santiago@thinksid.co` figura como «Santiago Admin». Las dos fuentes candidatas apuntan
  hoy a la misma cuenta. **No se usó eso para reparar nada** — sigue siendo decisión tuya.
- **La PARTE C del #61** (que el camino `captura_david` reconfirme la cantidad) es un cambio de
  comportamiento aparte.
- **`secado_due`** tiene exactamente la misma forma que el defecto del #89 — afirma «hoy» sin comparar
  contra la fecha de referencia — y **no se tocó**, porque no tiene evidencia propia todavía.
- **Ningún umbral de gravedad se cambió.** Las cinco copias coincidían en 10/30. Ese número es contrato
  de negocio y es tuyo.

---

## Deriva de migraciones

**Cero migraciones aplicadas-sin-fusionar.** Las nueve de `133`–`141` tienen fichero en `main` y fila en
el ledger. La ventana que vigila el lunes está limpia.

**La deriva que sí existe va en la dirección contraria**, y es evidencia nueva anexada al **#69** ya
abierto (como bloque de cuerpo, sin sobrescribir su `Evidencia`): tres migraciones documentadas como
«SIN APLICAR» que producción sí corre.

| Migración | `CLAUDE.md` raíz | Ledger | Catálogo vivo |
|---|---|---|---|
| 133 `insert_update_por_rol` | l. 342 «ESCRITA, FUSIONADA Y SIN APLICAR» | `20260901022335` | barrido always-true INSERT/UPDATE = **0** (la ficha dice 33 sobre 20 tablas) |
| 137 `get_user_role_respeta_activo` | l. 357 «ESCRITA, SIN APLICAR» | `20260908160300` | `get_user_role` **sí** filtra `activo` |
| 141 `venta_ganado_peso_destare` | l. 376 «ESCRITA, SIN APLICAR» | `20260909130404` | las 2 columnas nuevas existen |

**Las dos primeras son endurecimientos de RLS**, así que el coste no es cosmético: un agente que lea el
`CLAUDE.md` y le crea concluye que sigue abierto un agujero de INSERT/UPDATE sobre 20 tablas y que una
cuenta desactivada pasa ~125 políticas. Las dos cosas son falsas hoy.

---

## Pase de antigüedad (Phase 4)

**Vacío, y ése es el resultado correcto.** El hallazgo abierto más viejo es del 2026-08-24 (#45 y #48),
o sea **18 días**; el umbral del runbook son 60. **Nada que proponerte como «aceptado, no se arregla»
esta semana.**

---

## Phase 0 — arranque

| Conector | Tool | Resultado |
|---|---|---|
| `Supabase` (solo lectura) | `execute_sql` | OK — `supabase_read_only_user`, `default_transaction_read_only = on` |
| `Supabase_Escritura` | `apply_migration` | OK |
| `Notion` | `notion-fetch`, `notion-query-data-sources`, `notion-update-page`, `notion-create-pages` | OK |
| `github` | `list_pull_requests`, `create_pull_request` | OK |
| Composio / Vercel | no probado | el drenaje del viernes no lee Vercel |

- **Dead-man check**: corrida anterior `2026-09-10-jueves`, ayer. Brecha de 1 día. Sano.
- **Árbol**: `HEAD` desprendido en `0aba907`, rama local `main` 46 commits atrás. Alineado **antes** de
  despachar a nadie. **Error propio anotado**: se usó `checkout main` + `reset --hard` en vez de
  `git branch -f main origin/main`, que es lo que fija la lección del 2026-09-03. Sin consecuencia
  porque no había agentes en vuelo — que es exactamente la condición bajo la cual la lección lo tolera.
  Fue suerte de secuencia, no disciplina.
- **Aislamiento**: 4 worktrees, uno por agente, `node_modules` por symlink, un solo `npm ci` previo.

## Conjunto elegible — cómo se eligió

Regla mecánica: `Severidad ∈ {P2,P3}` · `Estado = Not started` · `Confianza = Alta` ·
`Requiere aprobacion` sin marcar · `Clase ∈ {codigo, ddl_aditivo}`. De **20 abiertos**, **9 elegibles**
(8 `codigo`, 1 `ddl_aditivo`).

Descartados por la regla, sin juicio: #87 (P1) · #45 #65 #68 #76 #85 #90 (requieren aprobación o son
`decision`) · #70 #91 (Confianza Media) · #56 #88 (`datos`).

**Los 8 `codigo` elegibles son todos P2 y todos esfuerzo S**, así que `Severidad × Esfuerzo⁻¹` los
empata a los ocho. El desempate fue el orden de prioridades permanentes de la constitución §2 — Hato
Lechero (#89), Inventario (#61), Monitoreo (#48) — y a igualdad de módulo, la antigüedad. El #48 era
además el elegible más viejo del tablero.

---

## NO CORRIÓ

- **Vercel / Composio** — fuera del alcance del drenaje del viernes, que no lee despliegues. No es un
  fallo.
- Ninguna especialidad quedó sin correr por fallo de herramienta. **Cero prompts de permiso en toda la
  corrida.**
