# Escocia OS — corrida `2026-09-25-viernes` · modo: full write

Drenaje del backlog. Preflight **9/9 en verde, cero prompts de permiso**. **Cero escrituras
a la base de datos**: el carril `ddl_aditivo` no se usó esta corrida porque ningún hallazgo
elegible lo pedía.

---

## RESUMEN (ES)

**El tablero dejó de ser un problema de capacidad y pasó a ser un problema de decisiones
tuyas.** Hay siete fichas abiertas y **sólo una era del viernes**: la del botón «Actualizar»
de Clima, que pediste el martes. Está hecha y con PR abierto. De las otras seis, **cinco
esperan una decisión tuya** y una espera evidencia, no trabajo. El cupo del viernes es de 5
PRs y se usó 1, así que no quedó nada represado por falta de manos.

**Eso invierte el motivo por el que existe el viernes.** Se creó porque el backlog se
llenaba más rápido de lo que se drenaba; hoy el drenaje sobra y el cuello son las cinco
fichas que sólo tú puedes resolver — dos de finca (el Drench de septiembre y la fecha de
parto de COPITA), tres de proceso.

**Nada envejecido, nada aplicado sin fusionar.** La ficha abierta más vieja tiene 7 días, no
hay ninguna con 60+ días para proponerte cerrar, y todas las migraciones aplicadas hasta la
166 tienen su archivo en `main` — la ventana entre aplicar y fusionar está limpia.

**P0: 0 · P1: 0 · P2: 0 · P3: 1  (nuevos)  |  cerrados: 0 · drenados: 1**

**El único hallazgo nuevo (ESCO-133) es del tipo que el viernes sí puede filar**: el que la
propia corrección causa. El botón nuevo agrega un tercer disparador del backfill de clima —
el primero que se alcanza desde el navegador — y esa entrada nueva es la que queda sin
candado entre instancias. No es un barrido: el viernes no abrió ninguna investigación.

---

## DRENADO

- **1 hallazgo → PR abierto** · **0 migraciones aplicadas** · **0 cerrados por obsoletos**
- **1 hallazgo nuevo filado (ESCO-133)** — causado por la corrección, no por un barrido
- **Quedan 0 elegibles sin tocar.** El cupo (5 `codigo` + 1 `ddl_aditivo`) no se agotó: el
  conjunto elegible era de uno solo. No hubo truncamiento.

## APLICADO SIN FUSIONAR

- Ninguna. Se verificaron las 12 migraciones aplicadas desde el 2026-09-14 (150–166) contra
  los archivos de `src/sql/migrations/` en `main`: **las 12 tienen archivo**. No hay ninguna
  corriendo en producción sin estar documentada en `main`.

---

## PRs ABIERTOS

- **feat(clima): add a one-click "Actualizar" button that repairs the last 7 incomplete days**
  → https://github.com/sforero94/Escociaos/pull/291 (ESCO-127)
  9 archivos, +1.134/−3. Endpoint nuevo `POST /clima/actualizar` en los dos árboles de edge
  function, lógica pura con 26 pruebas nuevas, botón sólo para Gerencia.
  Verificación reportada por el agente: **188 archivos / 3.900 pruebas en verde**
  (línea base `main`: 187 / 3.873), `tsc --noEmit` en 0, lint **0 errores / 919 avisos**,
  idéntico a la línea base. `mergeable_state: clean`.
  **Requiere `npx supabase functions deploy make-server-1ccce916` después de fusionar** —
  hasta entonces el botón responde 404 y no escribe nada.

### Dos divergencias deliberadas del PR, para que no te sorprendan

1. **El día en curso queda FUERA de la ventana de 7 días**, aunque la acción filada decía
   «desde hoy hasta 7 días atrás». Motivo: `backfillUnDia` **borra** las lecturas del rango
   que va a cubrir antes de insertar, y el día de hoy todavía no tiene fila, así que la
   guarda de no-empeorar no tiene contra qué comparar y deja pasar el reemplazo. Un clic
   habría cambiado las lecturas vivas de 5 minutos de hoy por lo que la History API tenga en
   ese momento. La ventana son los 7 días completos que terminan ayer, anclada en Bogotá.
   Si lo querés al revés, es un cambio de una línea.
2. **`contador_congelado` no entra en la selección**: es firmware del sensor, no un hueco de
   captura, y el cron de la 121 ya lo reintenta a diario. También es una línea si lo querés
   dentro.

### Lo que el botón no va a arreglar todavía

Los dos días que hoy siguen incompletos — **16-sep (11 lecturas) y 17-sep (189)** — ya
quedaron fuera de la ventana de 7 días. Son cortes reales, no un defecto. Si los querés
recuperar hay que pedirlo en una sesión, y conviene antes de mediados de diciembre, cuando
Ecowitt deje de servirlos a 5 minutos.

---

## REQUIERE TU DECISIÓN

Cinco de las seis fichas que el viernes no puede tocar están esperándote a ti. Van en orden
de lo que cuesta dejarlas quietas.

1. **Drench Septiembre sigue sin poder cerrarse** (ESCO-129, P2, `clase: datos`)
   24 días abierta. 59,60 kg de Nutrifeed menor anotados contra 59,00 kg de existencias, y
   `fn_cerrar_aplicacion` cancela el cierre entero. **Mientras no cierre no existe el
   registro GlobalGAP del drench de septiembre.** La pregunta sigue siendo la misma que el
   jueves: ¿falta una compra, o sobra consumo anotado? El Fosfato monopotásico va por el
   mismo camino con 1,2 kg de margen.

2. **COPITA: su parto figura en mayo de 2027** (ESCO-128, P2, `clase: datos`)
   La migración 163 borró el servicio falso pero no tocó las columnas normalizadas que lee
   `v_hato_estado_actual`. La alerta de parto dispara 14 días antes de la fecha almacenada,
   así que **no va a sonar**, y el parto real es en unas seis semanas.

3. **¿Pongo un gate de pruebas en los PR?** (ESCO-132, P2, `clase: codigo`, requiere tu
   aprobación)
   Sigue sin haberlo: el **único** check del PR #291 de hoy es «Vercel Preview Comments».
   Ni lint, ni typecheck, ni pruebas. El agente corrió las tres a mano en su worktree, que
   es precisamente la red que el repositorio no tiene.

4. **ESCO-114 lista para cerrar como *Arreglado*.** El preflight de hoy volvió a confirmar,
   de forma independiente, que `SUPABASE_APPLY_A_MIGRATION` resuelve en la cuenta
   `escocia-os`. Es la tercera confirmación. Tiene `Requiere aprobación` marcado, así que no
   la cierro yo.

5. **El candado de clima entre instancias** (ESCO-133, P3, `clase: ddl_aditivo`)
   Nada que decidas hoy: queda elegible para el drenaje del viernes que viene, que puede
   aplicar el RPC `pg_try_advisory_lock` por su carril normal de cinco puertas. Lo menciono
   sólo para que sepas que el PR #291 se embarca con ese hueco conocido y acotado.

6. **El carril de logs sigue muerto** (ESCO-131, P2, `clase: decision`)
   `SUPABASE_GET_PROJECT_LOGS` pega contra el endpoint retirado `logs.all` y devuelve 410 en
   toda consulta. Hay que decidir el reemplazo; no es trabajo de código.

7. **Recordatorio de calendario**: el **1 de noviembre** EE. UU. vuelve al horario estándar
   y las tres Routines (`0 11 * * 1|4|5` UTC) pasan de las 7:00 a las 6:00. Hay que moverlas
   a `0 12 * * 1|4|5`. Faltan cinco semanas.

---

## POR QUÉ SÓLO UNA FICHA ERA ELEGIBLE

La elegibilidad es mecánica, sin juicio. De las 7 abiertas:

| ID | Sev | Clase | Conf. | Req. aprob. | ¿Viernes? | Qué la bloquea |
|---|---|---|---|---|---|---|
| ESCO-127 | P2 | codigo | Alta | no | **sí** | — (drenada, PR #291) |
| ESCO-114 | P1 | decision | Alta | sí | no | P1 + decisión + tu aprobación |
| ESCO-132 | P2 | codigo | Alta | **sí** | no | tu aprobación (cambia tu forma de trabajar) |
| ESCO-131 | P2 | decision | Alta | no | no | `clase: decision` |
| ESCO-130 | P2 | codigo | **Media** | no | no | **evidencia**, no decisión |
| ESCO-128 | P2 | datos | Alta | sí | no | `clase: datos` + tu aprobación |
| ESCO-129 | P2 | datos | Alta | sí | no | `clase: datos` + tu aprobación |
| ESCO-133 | P3 | ddl_aditivo | Alta | no | (nueva hoy) | elegible el viernes que viene |

**ESCO-130 es la única que no espera una firma tuya**: quedó en `Confianza: Media` y necesita
que alguien la confirme o la refute. El viernes no investiga, así que se queda para el lunes.

## PASE DE ENVEJECIMIENTO (Phase 4)

**Nada que proponerte.** Ninguna ficha abierta P2/P3 lleva 60+ días sin tocarse: la más
vieja es ESCO-114, del 2026-09-18 (7 días). El tablero está sano.

## REGLA DE AUTO-PODA

**No se dispara.** El conjunto elegible no estaba vacío (1 elegible), así que la racha de
viernes vacíos sigue en 0. El viernes se queda semanal.

---

## NO CORRIÓ

Nada. Preflight completo sin un solo prompt de permiso:

| Comprobación | Resultado |
|---|---|
| `COMPOSIO_MANAGE_CONNECTIONS` (supabase, notion, vercel, github) | 4/4 `active` |
| `SUPABASE_RUN_READ_ONLY_QUERY` · `SUPABASE_APPLY_A_MIGRATION` | resuelven |
| `NOTION_QUERY_DATABASE_WITH_FILTER` · `NOTION_UPDATE_ROW_DATABASE` · `NOTION_FETCH_DATABASE` | resuelven |
| Cuenta `escocia-os` (`supabase_bitis-coward`) | activa, **no es la predeterminada** — pineada en toda llamada |
| Dead-man (corrida previa 2026-09-24-jueves) | 1 día de brecha |
| Modo de escritura | full write (push de rama verificado) |
| Árbol de trabajo / worktree | 1 worktree, `node_modules` enlazado, un solo `npm ci` |

---

## ADENDA — 12:50 UTC, 65 minutos después de cerrar la corrida

**El PR #291 se cerró sin fusionar, y no fue un rechazo: lo reemplazó el #292.** Una sesión
en vivo con Santiago, posterior a esta corrida, tomó el commit `56ef9a9` **sin cambios** y le
agregó encima el resto. El #292 está abierto sobre `b756a01`, o sea sobre el commit de
memoria de esta corrida.

**Verificado contra el ledger, no contra el comentario del PR** — tres migraciones aplicadas
a producción a las 12:42–12:43 UTC:

| # | Ficha | Ledger |
|---|---|---|
| 167 `copita_chequeo_normalizado` | ESCO-128 | `20260925124250` |
| 168 `ajuste_nutrifeed_drench_septiembre` | ESCO-129 | `20260925124320` |
| 169 `clima_candado_backfill` | ESCO-133 | `20260925124346` |

O sea que **las tres decisiones de finca que este reporte puso en REQUIERE TU DECISIÓN se
resolvieron el mismo día**, y ESCO-133 —filado a las 11:36— quedó resuelto a las 12:43.

### Lo que corregí, y lo que deliberadamente no toqué

La sesión en vivo **no actualizó Notion**: ESCO-127 y ESCO-133 seguían con la marca de tiempo
de mis propias ediciones. Corregí **sólo las dos filas que esta corrida creó o editó**:

- **ESCO-127** → PR re-apuntado al #292.
- **ESCO-133** → `In progress`, PR #292, con el motivo de cierre.
  **Esto era lo urgente**: estaba en `Not started`, `ddl_aditivo`, `Confianza: Alta` y sin
  aprobación, o sea **elegible para el drenaje del viernes que viene**, que habría intentado
  escribir de nuevo una migración que ya existe.

**No toqué ESCO-128/129/130/131/132.** Son de esa sesión, varias son `clase: datos` con
aprobación tuya, y no me corresponde juzgar si su verificación cerró.

### Mi recomendación en ESCO-133 estaba equivocada en el mecanismo

Propuse `pg_try_advisory_lock`. **El #292 usó un lease en tabla, y tiene razón**: PostgREST
usa un pool de conexiones, así que un advisory lock de sesión se queda pegado a una conexión
y uno de transacción se suelta al terminar el RPC. Ninguno de los dos sirve. Queda en el
ledger de refutaciones.

### Riesgo abierto para el viernes que viene

El #292 trae un cambio al hook de permisos de Supabase con esta consecuencia, que su propio
cuerpo declara: **si el hook carga en una Routine desatendida, `SUPABASE_APPLY_A_MIGRATION`
pasa a PEDIR permiso, y el carril `ddl_aditivo` del viernes se muere en ese prompt.** Lo
aprobaste a sabiendas y el 2026-09-24 el hook no cargó en las Routines, así que hoy es
condicional. **El preflight del próximo viernes tiene que comprobarlo explícitamente**: si
pide permiso, el carril no corre y va como NO CORRIÓ, nunca esperando.
